{
  "name": "food-court",
  "version": "1.0.0",
  "description": "Food Court — Mais que delivery. Um novo ecossistema de alimentação.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test --test-concurrency=1",
    "lint": "cd .. && node Backend/node_modules/eslint/bin/eslint.js --config eslint.config.js Backend/src frontend/js Backend/scripts Backend/test eslint.config.js",
    "format:check": "prettier --check package.json test",
    "check": "npm run lint && npm test && npm run format:check"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "nodemailer": "^9.0.6",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.9.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  }
}
