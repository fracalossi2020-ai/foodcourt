import { api } from '../core/api.js'
import { esc } from '../core/ui.js'
export async function render(view){
  view.innerHTML='<div class="partner-loading">Carregando seus benefícios...</div>'
  try{const data=await api.loyalty();const next=data.next;const start=data.level==='Bronze'?0:data.level==='Prata'?500:data.level==='Ouro'?1500:3000;const progress=next?Math.min(100,(data.points-start)/(next.min-start)*100):100;view.innerHTML=`<div class="page customer-feature-page"><a class="profile-back" href="#/perfil">← Voltar ao perfil</a><header class="loyalty-hero"><div><span>FOODCOURT CLUB</span><h1>Nível ${esc(data.level)}</h1><p>Você tem <b>${data.points} pontos</b>${next?` e faltam ${Math.max(0,next.min-data.points)} para ${next.name}`:' e chegou ao nível máximo'}.</p><div class="loyalty-progress"><i style="width:${progress}%"></i></div></div><strong>🏅</strong></header><section class="section"><div class="section-head"><div><h2>Missões</h2><div class="sub">Complete desafios e ganhe pontos.</div></div></div><div class="mission-grid">${data.missions.map(m=>`<article class="mission-card"><span>+${m.reward} pontos</span><h3>${esc(m.title)}</h3><p>${Math.min(m.progress,m.target)} de ${m.target} concluídos</p><div><i style="width:${Math.min(100,m.progress/m.target*100)}%"></i></div></article>`).join('')}</div></section><section class="section"><div class="section-head"><h2>Histórico de pontos</h2></div><div class="card loyalty-history">${data.events.length?data.events.map(e=>`<div><span>✨</span><p><b>${esc(e.label)}</b><small>${new Date(e.at).toLocaleDateString('pt-BR')}</small></p><strong>+${e.points}</strong></div>`).join(''):'<p class="partner-empty">Faça pedidos e avaliações para começar a pontuar.</p>'}</div></section></div>`}catch(error){view.innerHTML=`<div class="state-box"><h3>Não foi possível carregar</h3><p>${esc(error.message)}</p></div>`}
}
